{
  "name": "@vtex/tsconfig",
  "version": "0.4.2",
  "publishConfig": {
    "access": "public"
  },
  "main": "tsconfig.json",
  "license": "MIT",
  "scripts": {
    "postversion": "chan release $npm_package_version && git add CHANGELOG.md && git commit --amend --no-edit"
  },
  "peerDependencies": {
    "typescript": "^3.7.5"
  }
}
